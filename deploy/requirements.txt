# Python deps for the SailFrames REST API (backend/). Debian-slim base.
# Heavy data-processing deps (numpy/pandas/ffmpeg) live in the worker images,
# not here — the API only serves precomputed results.

fastapi>=0.110
uvicorn[standard]>=0.29
boto3>=1.28               # S3 / MinIO blob store
requests>=2.31           # dispatch ingest events to the worker RIE endpoints
python-multipart>=0.0.9  # GPX UploadFile endpoint in backend/routers/races.py
nh3>=0.2.18              # HTML sanitizer for rich-text prose (backend/richtext.py)

# Postgres metadata backend
sqlalchemy>=2.0
psycopg[binary]>=3.1
alembic>=1.13            # schema migrations

# Auth: Argon2id password hashing + JWT access tokens
argon2-cffi>=23.1
pyjwt>=2.8
